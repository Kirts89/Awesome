let initialState = {
  items: [],
}

const postsReducer = (state = initialState, action) => {
  // state #=> {items: [{}], current: {}}
  switch (action.type) {
    default:
      return state
  }
}

export default postsReducer